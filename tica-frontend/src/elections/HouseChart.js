import React from 'react';
import { Box } from '@mui/material';
import { TicaHouseGrid } from "./Representatives";

const HouseChart = ({ redReps, blueReps, purpleReps }) => {
  return (
    <Box>
      no more this either
    </Box>
  );
};

export default HouseChart;
