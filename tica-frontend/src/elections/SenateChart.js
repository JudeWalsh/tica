import React from 'react';
import { Box } from '@mui/material';

const SenateChart = ({ redSenators, blueSenators, purpleSenators }) => {
  return (
    <Box>
      no more
    </Box>
  );
};

export default SenateChart;
