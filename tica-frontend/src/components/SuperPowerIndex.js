import React from 'react';
import GlobalMap from "../SPI/GlobalMap";
import { Box, Typography } from "@mui/material";

const SuperPowerIndex = () => {

  return (
    <Box sx={{ width: "100%", height: "100vh"}}>
      <GlobalMap/>
    </Box>
  );
};

export default SuperPowerIndex;
